## 专业术语解析
- DOM：Document Object Model，文档对象模型，是浏览器渲染网页时构建的树状结构，每个节点都是一个对象，通过DOM可以操作网页的结构、样式和内容。
```html
<!-- 你在网页上看到的是一个搜索框 -->
 <input type="text" placeholder="搜索">
 <button>搜索</button>
```
DOM下就是这样：
```html
<!-- DOM树 -->
DOM={
    input:{id:"search",type:"text",placeholder:"请输入关键词"},
    button:{id:"submit",text:"搜索"}
}
```
- **CSS选择器**：css选择器可以通过标签名，id，类名等组合找到网页上的某个按钮或者输入框
- **viewpoint视口**
- **iframe**：内联框架，一个网页嵌套另一个网页，用于隔离和保护内容
- **幂等**：做多少次，结果都是一样的，不会因为次数不同而改变结果，日志初始化，数据库建表都设计成幂等，防止代码不小心多次调用出现重复创建的问题
```python
# 幂等操作：最终调用多少次，_INITIALIZED最终都是True
def setup_logger():
    global _INITIALIZED
    if _INITIALIZED:
        return logger # 已经初始化过，直接返回logger
        # ...初始化逻辑
    _INITIALIZED = True
    return logger

# 这个是非幂等操作，每次调用结果都不同
counter=0
def increment():
    global counter
    counter += 1
```
- **装饰器**：给函数加特效
- **单例模式**：所有代码公用某一个类的同一个对象
- **playwright**：浏览器自动化框架
- **stealth**：反检测
- **CDP**：
    - launch模式：自己启动一个新的浏览器
    - cdp模式：连接已经打开的edge/chrome浏览器
- **多模态**
- **温度参数**：0-1之间的浮点数，控制大模型思考的发散程度
- **冒烟测试**：测试模块的基本功能，确保在正常情况下能够正常运行
- **RPA**：AI智能决策
- **ORM**：Object-Relational Mapping（对象关系映射），将数据库表映射为Python对象，方便操作数据库
- **Base64编码**：将图片、视频转成文字，给到大模型
- **上下文**：广义的上下文
    - 浏览器的上下文，标签页，缓存和cookie
    - ai context
    - 循环context：任务执行过程中的所有变量


### 项目总览
Web Agent是一款桌面端的AI浏览器的智能PRA，用户只需要用自然语言描述想要完成的任务（自然语言指令”在bing中搜索openai，并打开第一条搜索结果“），系统就能自主规划：
1. 自动启动浏览器
2. 调用当前界面的状态
3. 调用大模型
4. 执行浏览器
5. 循环以上步骤，直到任务完成


**核心技术点**
- 双模态感知：采集DOM结构化数据+页面截图，为大模型提供上下文信息
- 双兜底运行：优先DOM精准定位，失败时自动回落视觉坐标点击
- 反检测：playwright-stalth抹掉自动化指纹
- 状态机驱动：基于transition库的有限状态机控制agent循环
- 全链路留痕：每步执行操作持久化到sqlite数据库，含截图回溯
- 桌面GUI：CustomTkinter暗黑主题的界面

### 技术栈
| 类别     | 技术                      | 用途                              |
| ------ | ----------------------- | ------------------------------- |
| 浏览器引擎  | Playwright              | 浏览器自动化控制                        |
| 反检测    | playwright-stealth      | 抹掉 `navigator.webdriver` 等自动化指纹 |
| 桌面窗口控制 | pyautogui + pygetwindow | 窗口激活/置顶、坐标兜底点击                  |
| AI 决策  | OpenAI SDK（兼容协议）        | 调用 Qwen-VL / GPT-4o 等多模态大模型     |
| 状态机    | transitions             | Agent 循环的有限状态机                  |
| 桌面 GUI | CustomTkinter           | 现代暗色主题桌面界面                      |
| 日志     | loguru                  | 高性能日志（控制台 + 文件轮转）               |
| 数据校验   | Pydantic v2             | 配置解析 + 数据结构定义                   |
| 数据库    | SQLAlchemy 2.x + SQLite | 任务/步骤持久化                        |
| 重试     | tenacity                | LLM 调用的指数退避重试                   |
| 图像     | Pillow                  | 截图压缩处理                          |
| 打包     | PyInstaller             | 打包为独立 EXE                       |
| 包管理    | uv                      | 快速依赖安装                          |

### 架构设计
| 层级 | 功能 |
| --- | --- |
| core层 | 核心业务逻辑，负责任务规划、状态管理、大模型调用等 |
| executor层 | 执行浏览器操作，负责点击DOM元素、输入文本等 |
| perception层 | 采集DOM结构化数据+页面截图，为大模型提供上下文信息 |
| agent层 | 基于transition库的有限状态机，负责agent循环 |
| ui层 | 桌面GUI，负责用户交互、任务管理、状态显示等 |

> 关键点：数据是自上向下流动的
> **agent**：”感知-思考-执行“循环

**层级：**
- ui层（CustomTkinter暗黑主题的界面）
- agent层（VLMClient/AgentLoop）
- perception层（PerceptionCollector）
- executor层（BrowserManager/ActionExecutor）
- core层（配置/loguru/数据结构/异常/持久化/仓库）


---

## 配置环境

1. 安装依赖：

```bash
# 使用 uv 初始化项目（自动创建 pyproject.toml 和 .venv）
uv init
```
**pyproject.toml文件内容**：

```toml
[project]
name = "webagent"
version = "0.1.0"
description = "云端多模态AI浏览器智能PRA"
requires-python = ">=3.10"
authors = [{ name = "neonsaya" }]
license = { text = "Proprietary" }
readme = { content-type = "text/markdown", text = "WebAgent 商用桌面端" }

# ---------------- 生产依赖（运行时必装） ----------------
dependencies = [
    # 浏览器引擎 & 反检测
    "playwright>=1.47.0",
    "playwright-stealth>=1.0.6",

    # 桌面窗口控制
    "pyautogui>=0.9.54",
    "pygetwindow>=0.0.9",
    "pillow>=10.4.0",

    # AI 决策 & 多模态客户端（OpenAI 兼容协议）
    "openai>=1.40.0",
    "langchain>=0.2.16",
    "langchain-core>=0.2.40",
    "transitions>=0.9.2",

    # 桌面 GUI
    "customtkinter>=5.2.2",

    # 工程基础
    "loguru>=0.7.2",
    "pydantic>=2.8.2",
    "pydantic-settings>=2.4.0",
    "python-dotenv>=1.0.1",
    "sqlalchemy>=2.0.32",
    "tenacity>=8.5.0",
    "httpx>=0.27.0",
]

# ---------------- 开发依赖（不打入 EXE） ----------------
[dependency-groups]
dev = [
    "pyinstaller>=6.10.0",
    "ruff>=0.6.4",
    "pytest>=8.3.2",
]

# ---------------- UV 配置 ----------------
# 关闭包安装：避免在 Windows 中文路径下生成带中文绝对路径的 .pth 文件
[tool.uv]
package = false

# ---------------- Ruff 风格 ----------------
[tool.ruff]
line-length = 110
target-version = "py310"

# 清华源
[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

```

```bash
# 安装所有依赖（包括开发依赖）
uv sync --all-groups

# 安装 Playwright 浏览器内核（必须！否则无法启动浏览器）
uv run playwright install chromium
```

测试环境是否部署完成：
```bash
uv run python -c "import playwright; print('playwright OK')"
uv run python -c "import customtkinter; print('customtkinter OK')"
uv run python -c "import openai; print('openai OK')"
```
3. 配置.env和.gitignore
.env
```
# =========================================================
# WebAgent 环境变量样例
# 使用前复制为 .env 并按需修改；运行时由 pydantic-settings 自动加载
# =========================================================

# ---------------- VL 大模型（OpenAI 兼容协议） ----------------
# 支持任意兼容 OpenAI Chat Completions 协议的多模态模型：
#   - OpenAI GPT-4o:       https://api.openai.com/v1
#   - 通义千问 Qwen-VL:    https://dashscope.aliyuncs.com/compatible-mode/v1
#   - DeepSeek-VL:         https://api.deepseek.com/v1
WEBAGENT_LLM__BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
WEBAGENT_LLM__API_KEY=sk-your-key-here
WEBAGENT_LLM__MODEL=qwen-vl-max-latest
WEBAGENT_LLM__TEMPERATURE=0.2
WEBAGENT_LLM__TIMEOUT=60

# ---------------- Agent 运行参数 ----------------
WEBAGENT_AGENT__MAX_STEPS=25
WEBAGENT_AGENT__STEP_INTERVAL_MS=600

# ---------------- 浏览器 ----------------
# launch:    每次启动新的 Playwright Chromium
# cdp:       通过 CDP 接管已打开的 Chrome/Edge（推荐）
WEBAGENT_BROWSER__MODE=launch
WEBAGENT_BROWSER__CDP_URL=http://127.0.0.1:9222
WEBAGENT_BROWSER__HEADLESS=false

# ---------------- 日志 ----------------
WEBAGENT_LOG__LEVEL=INFO
```
WEBAGENT和Pydantic配合，实现环境变量的嵌套配置。
**launch vs cdp**
launch:不需要登录的浏览器操作
cdp:需要登录状态（管理后台），客户展示


.gitignore
```text
# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp

# UV
.uv-cache/

# 项目运行时产物
data/*.db
data/*.sqlite
logs/
screenshots/
.pytest_cache/
.ruff_cache/
```

### 核心基础设施层Core
    - 配置管理
    - 日志系统
    - 异常体系
    - 数据结构
    - 数据库

## config.py 代码解析

### 整体作用
config.py 是整个项目的"配置中心"，把所有配置项（大模型地址、浏览器参数、日志级别等）集中管理，通过 `pydantic-settings` 自动从 `.env` 文件加载，类型自动校验。

### 关键知识点

**1. `from __future__ import annotations`**
- 让类型注解（如 `-> Settings`）在运行时不求值，只在类型检查时生效
- 解决"类还没定义完就引用自己"的循环引用问题

**2. `Path(__file__).resolve().parents[2]`**
- `__file__`：当前文件路径（core/config.py）
- `.resolve()`：转成绝对路径
- `.parents[2]`：往上跳2级目录（core/ → webagent/ → 项目根）
- 作用：不管从哪里运行代码，都能准确定位到项目根目录

**3. 嵌套配置类设计**
```
Settings（根配置）
├── LLMSettings      → 大模型配置（base_url, api_key, model...）
├── BrowserSettings  → 浏览器配置（headless, cdp_url...）
├── AgentSettings    → Agent运行参数（max_steps, step_interval...）
└── LogSetting       → 日志配置（level, rotation, retention）
```
- 每个子类继承 `BaseSettings`，都能独立从环境变量读取
- `Literal['launch','cdp']`：限制字段只能取这两个值之一
- `str | None = None`：可选字段，不填就是 None

**4. `Field(default_factory=...)` 的作用**
```python
# ❌ 错误写法：类定义时就创建了实例，所有 Settings 共享同一个子对象
agent: AgentSettings = AgentSettings()

# ✅ 正确写法：每次创建新 Settings 时才创建新的 AgentSettings
agent: AgentSettings = Field(default_factory=AgentSettings)
```
`default_factory` 接收一个"工厂函数"，每次需要默认值时调用它，而不是用同一个值。

**5. `SettingsConfigDict` 环境变量映射**
```python
model_config = SettingsConfigDict(
    env_file=PROJECT_ROOT/".env",        # 读取哪个 .env 文件
    env_prefix="WEBAGENT_",              # 环境变量前缀
    env_nested_delimiter="__",           # 嵌套分隔符
)
```
映射规则：`WEBAGENT_AGENT__MAX_STEPS` → `Settings().agent.max_steps`
- `WEBAGENT_`：前缀（被自动去掉）
- `AGENT`：对应 `Settings` 的 `agent` 字段
- `__`：嵌套分隔符（表示进入子对象）
- `MAX_STEPS`：对应 `AgentSettings` 的 `max_steps` 字段

**6. `@lru_cache(maxsize=1)` 实现单例**
```python
@lru_cache(maxsize=1)
def get_settings() -> Settings:
    return Settings()
```
- 第一次调用：执行函数体，创建 Settings 实例，缓存结果
- 之后调用：直接返回缓存的实例，不重复创建
- 整个程序生命周期只有一个 Settings 对象

**7. `d.mkdir(parents=True, exist_ok=True)`**
- `parents=True`：自动创建中间目录（如 `data/sub/` 会自动创建 `data/`）
- `exist_ok=True`：目录已存在也不报错（幂等操作）
- 在 `get_settings()` 里调用，确保程序启动时必要的目录都存在

## 全局配置管理-core/config.py

### 整体思路
把所有配置（大模型地址、浏览器参数、日志级别等）集中到一个 Python 对象里，通过 `pydantic-settings` 自动从 `.env` 文件加载，类型自动校验，不用到处写 `os.getenv()`。

### 目录常量（模块级变量）
```python
PROJECT_ROOT = Path(__file__).resolve().parents[2]  # 项目根目录
DATA_DIR  = PROJECT_ROOT / 'data'          # 数据库等运行时数据
LOG_DIR   = PROJECT_ROOT / 'logs'          # 日志文件
SCREENSHOT_DIR = PROJECT_ROOT / 'screenshots'  # 截图存储
```
- `Path(__file__).resolve().parents[2]`：从当前文件往上跳 2 级到项目根
- `SCREENSHOT_DIR` 的 `parents[2]` 原因：`core/config.py` → `core/` → `webagent/` → `项目根`
- 用 `pathlib.Path` 代替 `os.path`，跨平台兼容，写法更优雅

### 嵌套配置类设计
```
Settings（根）
  ├── llm:      LLMSettings       → 大模型配置
  ├── browser:  BrowserSettings   → 浏览器配置
  ├── agent:    AgentSettings     → Agent运行参数
  └── log:      LogSetting        → 日志配置
```
每个子类都继承 `BaseSettings`，职责分离，方便按模块管理配置。

### 字段定义方式
```python
# 直接赋默认值（非必填，.env 里不写就用默认值）
max_steps: int = 25

# Literal 限制取值范围（传错值会报类型错误）
mode: Literal['launch', 'cdp'] = 'launch'

# 可选字段（可以为空）
user_agent: str | None = None
```
注意：这些**不是硬编码**，因为继承了 `BaseSettings`，会被环境变量覆盖。

### 环境变量自动映射
```python
model_config = SettingsConfigDict(
    env_file=PROJECT_ROOT / ".env",   # 读取哪个 .env 文件
    env_file_encoding="utf-8",
    env_prefix="WEBAGENT_",           # 环境变量前缀
    env_nested_delimiter="__",        # 嵌套分隔符
    extra="ignore"                    # 忽略未声明的环境变量
)
```
**映射规则**：`env_prefix` + `子类名` + `分隔符` + `字段名`
```
.env 里写：  WEBAGENT_AGENT__MAX_STEPS=50
    ↓ 映射到
Settings.agent.max_steps = 50
```

### 用 `Field(default_factory=...)` 而非直接赋默认值的原因
```python
# ❌ 错误：类定义时执行一次，所有实例共享同一个子对象
agent: AgentSettings = AgentSettings()

# ✅ 正确：每次创建新实例时才创建子对象
agent: AgentSettings = Field(default_factory=AgentSettings)
```
`default_factory` 确保每次调用 `Settings()` 创建**全新的**子对象，避免多个实例意外共享数据。

### 单例模式：`lru_cache`
```python
@lru_cache(maxsize=1)
def get_settings() -> Settings:
    """全局唯一Setting实例（lru_cache缓存）"""
    for d in (DATA_DIR, LOG_DIR, SCREENSHOT_DIR):
        d.mkdir(parents=True, exist_ok=True)  # 幂等：目录已存在也不报错
    return Settings()
```
- `lru_cache(maxsize=1)`：函数第一次调用时执行，之后再调用直接返回缓存结果
- `d.mkdir(parents=True, exist_ok=True)`：确保 data/logs/screenshots 目录存在
- 整个程序生命周期内只创建一次 `Settings` 对象

**对比另一种单例写法**（模块级变量）：
```python
# Python 模块只会加载一次，所以模块级变量天然就是单例
settings = Setting()
# 使用：from core.config import settings
```
区别：模块级写法更简洁，但无法在创建前做初始化逻辑（如创建目录）。

## 异常体系-core/exceptions.py

## 日志系统-core/logger.py

## 数据结构定义-core/schemas.py
**核心数据结构**：
- action指令
- 感知结构
- 任务/步骤

- 统一的数据契约
    - 配合pydantic自动校验，非法数据在构造时就会报错，不至于运行时出类型错误
    - is_terminal() 方法：`done`和`error`是终态动作，状态机收到后停止循环

## 数据库持久化-core/storage.py
