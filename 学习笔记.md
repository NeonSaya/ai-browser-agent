## 专业术语解析
- DOM：Document Object Model，文档对象模型，是浏览器渲染网页时构建的树状结构，每个节点都是一个对象，通过DOM可以操作网页的结构、样式和内容。
```html
<!-- 你在网页上看到的是一个搜索框 -->
 <input type="text" placeholder="搜索">
 <button>搜索</button>
```
DOM下就是这样：
```html
<!-- DOM树 -->
DOM={
    input:{id:"search",type:"text",placeholder:"请输入关键词"},
    button:{id:"submit",text:"搜索"}
}
```
- **CSS选择器**：css选择器可以通过标签名，id，类名等组合找到网页上的某个按钮或者输入框
- **viewpoint视口**
- **iframe**：内联框架，一个网页嵌套另一个网页，用于隔离和保护内容
- **幂等**：做多少次，结果都是一样的，不会因为次数不同而改变结果，日志初始化，数据库建表都设计成幂等，防止代码不小心多次调用出现重复创建的问题
```python
# 幂等操作：最终调用多少次，_INITIALIZED最终都是True
def setup_logger():
    global _INITIALIZED
    if _INITIALIZED:
        return logger # 已经初始化过，直接返回logger
        # ...初始化逻辑
    _INITIALIZED = True
    return logger

# 这个是非幂等操作，每次调用结果都不同
counter=0
def increment():
    global counter
    counter += 1
```
- **装饰器**：给函数加特效
- **单例模式**：所有代码公用某一个类的同一个对象
- **playwright**：浏览器自动化框架
- **stealth**：反检测
- **CDP**：
    - launch模式：自己启动一个新的浏览器
    - cdp模式：连接已经打开的edge/chrome浏览器
- **多模态**
- **温度参数**：0-1之间的浮点数，控制大模型思考的发散程度
- **冒烟测试**：测试模块的基本功能，确保在正常情况下能够正常运行
- **RPA**：AI智能决策
- **ORM**：Object-Relational Mapping（对象关系映射），将数据库表映射为Python对象，方便操作数据库
- **Base64编码**：将图片、视频转成文字，给到大模型
- **上下文**：广义的上下文
    - 浏览器的上下文，标签页，缓存和cookie
    - ai context
    - 循环context：任务执行过程中的所有变量


### 项目总览
Web Agent是一款桌面端的AI浏览器的智能PRA，用户只需要用自然语言描述想要完成的任务（自然语言指令”在bing中搜索openai，并打开第一条搜索结果“），系统就能自主规划：
1. 自动启动浏览器
2. 调用当前界面的状态
3. 调用大模型
4. 执行浏览器
5. 循环以上步骤，直到任务完成


**核心技术点**
- 双模态感知：采集DOM结构化数据+页面截图，为大模型提供上下文信息
- 双兜底运行：优先DOM精准定位，失败时自动回落视觉坐标点击
- 反检测：playwright-stalth抹掉自动化指纹
- 状态机驱动：基于transition库的有限状态机控制agent循环
- 全链路留痕：每步执行操作持久化到sqlite数据库，含截图回溯
- 桌面GUI：CustomTkinter暗黑主题的界面

### 技术栈
| 类别     | 技术                      | 用途                              |
| ------ | ----------------------- | ------------------------------- |
| 浏览器引擎  | Playwright              | 浏览器自动化控制                        |
| 反检测    | playwright-stealth      | 抹掉 `navigator.webdriver` 等自动化指纹 |
| 桌面窗口控制 | pyautogui + pygetwindow | 窗口激活/置顶、坐标兜底点击                  |
| AI 决策  | OpenAI SDK（兼容协议）        | 调用 Qwen-VL / GPT-4o 等多模态大模型     |
| 状态机    | transitions             | Agent 循环的有限状态机                  |
| 桌面 GUI | CustomTkinter           | 现代暗色主题桌面界面                      |
| 日志     | loguru                  | 高性能日志（控制台 + 文件轮转）               |
| 数据校验   | Pydantic v2             | 配置解析 + 数据结构定义                   |
| 数据库    | SQLAlchemy 2.x + SQLite | 任务/步骤持久化                        |
| 重试     | tenacity                | LLM 调用的指数退避重试                   |
| 图像     | Pillow                  | 截图压缩处理                          |
| 打包     | PyInstaller             | 打包为独立 EXE                       |
| 包管理    | uv                      | 快速依赖安装                          |

### 架构设计
| 层级 | 功能 |
| --- | --- |
| core层 | 核心业务逻辑，负责任务规划、状态管理、大模型调用等 |
| executor层 | 执行浏览器操作，负责点击DOM元素、输入文本等 |
| perception层 | 采集DOM结构化数据+页面截图，为大模型提供上下文信息 |
| agent层 | 基于transition库的有限状态机，负责agent循环 |
| ui层 | 桌面GUI，负责用户交互、任务管理、状态显示等 |

> 关键点：数据是自上向下流动的
> **agent**：”感知-思考-执行“循环

**层级：**
- ui层（CustomTkinter暗黑主题的界面）
- agent层（VLMClient/AgentLoop）
- perception层（PerceptionCollector）
- executor层（BrowserManager/ActionExecutor）
- core层（配置/loguru/数据结构/异常/持久化/仓库）


---

## 配置环境

1. 安装依赖：

```bash
# 使用 uv 初始化项目（自动创建 pyproject.toml 和 .venv）
uv init
```
**pyproject.toml文件内容**：

```toml
[project]
name = "webagent"
version = "0.1.0"
description = "云端多模态AI浏览器智能PRA"
requires-python = ">=3.10"
authors = [{ name = "neonsaya" }]
license = { text = "Proprietary" }
readme = { content-type = "text/markdown", text = "WebAgent 商用桌面端" }

# ---------------- 生产依赖（运行时必装） ----------------
dependencies = [
    # 浏览器引擎 & 反检测
    "playwright>=1.47.0",
    "playwright-stealth>=1.0.6",

    # 桌面窗口控制
    "pyautogui>=0.9.54",
    "pygetwindow>=0.0.9",
    "pillow>=10.4.0",

    # AI 决策 & 多模态客户端（OpenAI 兼容协议）
    "openai>=1.40.0",
    "langchain>=0.2.16",
    "langchain-core>=0.2.40",
    "transitions>=0.9.2",

    # 桌面 GUI
    "customtkinter>=5.2.2",

    # 工程基础
    "loguru>=0.7.2",
    "pydantic>=2.8.2",
    "pydantic-settings>=2.4.0",
    "python-dotenv>=1.0.1",
    "sqlalchemy>=2.0.32",
    "tenacity>=8.5.0",
    "httpx>=0.27.0",
]

# ---------------- 开发依赖（不打入 EXE） ----------------
[dependency-groups]
dev = [
    "pyinstaller>=6.10.0",
    "ruff>=0.6.4",
    "pytest>=8.3.2",
]

# ---------------- UV 配置 ----------------
# 关闭包安装：避免在 Windows 中文路径下生成带中文绝对路径的 .pth 文件
[tool.uv]
package = false

# ---------------- Ruff 风格 ----------------
[tool.ruff]
line-length = 110
target-version = "py310"

# 清华源
[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

```

```bash
# 安装所有依赖（包括开发依赖）
uv sync --all-groups

# 安装 Playwright 浏览器内核（必须！否则无法启动浏览器）
uv run playwright install chromium
```

测试环境是否部署完成：
```bash
uv run python -c "import playwright; print('playwright OK')"
uv run python -c "import customtkinter; print('customtkinter OK')"
uv run python -c "import openai; print('openai OK')"
```
3. 配置.env和.gitignore
.env
```
# =========================================================
# WebAgent 环境变量样例
# 使用前复制为 .env 并按需修改；运行时由 pydantic-settings 自动加载
# =========================================================

# ---------------- VL 大模型（OpenAI 兼容协议） ----------------
# 支持任意兼容 OpenAI Chat Completions 协议的多模态模型：
#   - OpenAI GPT-4o:       https://api.openai.com/v1
#   - 通义千问 Qwen-VL:    https://dashscope.aliyuncs.com/compatible-mode/v1
#   - DeepSeek-VL:         https://api.deepseek.com/v1
WEBAGENT_LLM__BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
WEBAGENT_LLM__API_KEY=sk-your-key-here
WEBAGENT_LLM__MODEL=qwen-vl-max-latest
WEBAGENT_LLM__TEMPERATURE=0.2
WEBAGENT_LLM__TIMEOUT=60

# ---------------- Agent 运行参数 ----------------
WEBAGENT_AGENT__MAX_STEPS=25
WEBAGENT_AGENT__STEP_INTERVAL_MS=600

# ---------------- 浏览器 ----------------
# launch:    每次启动新的 Playwright Chromium
# cdp:       通过 CDP 接管已打开的 Chrome/Edge（推荐）
WEBAGENT_BROWSER__MODE=launch
WEBAGENT_BROWSER__CDP_URL=http://127.0.0.1:9222
WEBAGENT_BROWSER__HEADLESS=false

# ---------------- 日志 ----------------
WEBAGENT_LOG__LEVEL=INFO
```
WEBAGENT和Pydantic配合，实现环境变量的嵌套配置。
**launch vs cdp**
launch:不需要登录的浏览器操作
cdp:需要登录状态（管理后台），客户展示


.gitignore
```text
# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp

# UV
.uv-cache/

# 项目运行时产物
data/*.db
data/*.sqlite
logs/
screenshots/
.pytest_cache/
.ruff_cache/
```

### 核心基础设施层Core
    - 配置管理
    - 日志系统
    - 异常体系
    - 数据结构
    - 数据库

## config.py 代码解析

### 整体作用
config.py 是整个项目的"配置中心"，把所有配置项（大模型地址、浏览器参数、日志级别等）集中管理，通过 `pydantic-settings` 自动从 `.env` 文件加载，类型自动校验。

### 关键知识点

**1. `from __future__ import annotations`**
- 让类型注解（如 `-> Settings`）在运行时不求值，只在类型检查时生效
- 解决"类还没定义完就引用自己"的循环引用问题

**2. `Path(__file__).resolve().parents[2]`**
- `__file__`：当前文件路径（core/config.py）
- `.resolve()`：转成绝对路径
- `.parents[2]`：往上跳2级目录（core/ → webagent/ → 项目根）
- 作用：不管从哪里运行代码，都能准确定位到项目根目录

**3. 嵌套配置类设计**
```
Settings（根配置）
├── LLMSettings      → 大模型配置（base_url, api_key, model...）
├── BrowserSettings  → 浏览器配置（headless, cdp_url...）
├── AgentSettings    → Agent运行参数（max_steps, step_interval...）
└── LogSetting       → 日志配置（level, rotation, retention）
```
- 每个子类继承 `BaseSettings`，都能独立从环境变量读取
- `Literal['launch','cdp']`：限制字段只能取这两个值之一
- `str | None = None`：可选字段，不填就是 None

**4. `Field(default_factory=...)` 的作用**
```python
# ❌ 错误写法：类定义时就创建了实例，所有 Settings 共享同一个子对象
agent: AgentSettings = AgentSettings()

# ✅ 正确写法：每次创建新 Settings 时才创建新的 AgentSettings
agent: AgentSettings = Field(default_factory=AgentSettings)
```
`default_factory` 接收一个"工厂函数"，每次需要默认值时调用它，而不是用同一个值。

**5. `SettingsConfigDict` 环境变量映射**
```python
model_config = SettingsConfigDict(
    env_file=PROJECT_ROOT/".env",        # 读取哪个 .env 文件
    env_prefix="WEBAGENT_",              # 环境变量前缀
    env_nested_delimiter="__",           # 嵌套分隔符
)
```
映射规则：`WEBAGENT_AGENT__MAX_STEPS` → `Settings().agent.max_steps`
- `WEBAGENT_`：前缀（被自动去掉）
- `AGENT`：对应 `Settings` 的 `agent` 字段
- `__`：嵌套分隔符（表示进入子对象）
- `MAX_STEPS`：对应 `AgentSettings` 的 `max_steps` 字段

**6. `@lru_cache(maxsize=1)` 实现单例**
```python
@lru_cache(maxsize=1)
def get_settings() -> Settings:
    return Settings()
```
- 第一次调用：执行函数体，创建 Settings 实例，缓存结果
- 之后调用：直接返回缓存的实例，不重复创建
- 整个程序生命周期只有一个 Settings 对象

**7. `d.mkdir(parents=True, exist_ok=True)`**
- `parents=True`：自动创建中间目录（如 `data/sub/` 会自动创建 `data/`）
- `exist_ok=True`：目录已存在也不报错（幂等操作）
- 在 `get_settings()` 里调用，确保程序启动时必要的目录都存在

## 全局配置管理-core/config.py

### 整体思路
把所有配置（大模型地址、浏览器参数、日志级别等）集中到一个 Python 对象里，通过 `pydantic-settings` 自动从 `.env` 文件加载，类型自动校验，不用到处写 `os.getenv()`。

### 目录常量（模块级变量）
```python
PROJECT_ROOT = Path(__file__).resolve().parents[2]  # 项目根目录
DATA_DIR  = PROJECT_ROOT / 'data'          # 数据库等运行时数据
LOG_DIR   = PROJECT_ROOT / 'logs'          # 日志文件
SCREENSHOT_DIR = PROJECT_ROOT / 'screenshots'  # 截图存储
```
- `Path(__file__).resolve().parents[2]`：从当前文件往上跳 2 级到项目根
- `SCREENSHOT_DIR` 的 `parents[2]` 原因：`core/config.py` → `core/` → `webagent/` → `项目根`
- 用 `pathlib.Path` 代替 `os.path`，跨平台兼容，写法更优雅

### 嵌套配置类设计
```
Settings（根）
  ├── llm:      LLMSettings       → 大模型配置
  ├── browser:  BrowserSettings   → 浏览器配置
  ├── agent:    AgentSettings     → Agent运行参数
  └── log:      LogSetting        → 日志配置
```
每个子类都继承 `BaseSettings`，职责分离，方便按模块管理配置。

### 字段定义方式
```python
# 直接赋默认值（非必填，.env 里不写就用默认值）
max_steps: int = 25

# Literal 限制取值范围（传错值会报类型错误）
mode: Literal['launch', 'cdp'] = 'launch'

# 可选字段（可以为空）
user_agent: str | None = None
```
注意：这些**不是硬编码**，因为继承了 `BaseSettings`，会被环境变量覆盖。

### 环境变量自动映射
```python
model_config = SettingsConfigDict(
    env_file=PROJECT_ROOT / ".env",   # 读取哪个 .env 文件
    env_file_encoding="utf-8",
    env_prefix="WEBAGENT_",           # 环境变量前缀
    env_nested_delimiter="__",        # 嵌套分隔符
    extra="ignore"                    # 忽略未声明的环境变量
)
```
**映射规则**：`env_prefix` + `子类名` + `分隔符` + `字段名`
```
.env 里写：  WEBAGENT_AGENT__MAX_STEPS=50
    ↓ 映射到
Settings.agent.max_steps = 50
```

### 用 `Field(default_factory=...)` 而非直接赋默认值的原因
```python
# ❌ 错误：类定义时执行一次，所有实例共享同一个子对象
agent: AgentSettings = AgentSettings()

# ✅ 正确：每次创建新实例时才创建子对象
agent: AgentSettings = Field(default_factory=AgentSettings)
```
`default_factory` 确保每次调用 `Settings()` 创建**全新的**子对象，避免多个实例意外共享数据。

### 单例模式：`lru_cache`
```python
@lru_cache(maxsize=1)
def get_settings() -> Settings:
    """全局唯一Setting实例（lru_cache缓存）"""
    for d in (DATA_DIR, LOG_DIR, SCREENSHOT_DIR):
        d.mkdir(parents=True, exist_ok=True)  # 幂等：目录已存在也不报错
    return Settings()
```
- `lru_cache(maxsize=1)`：函数第一次调用时执行，之后再调用直接返回缓存结果
- `d.mkdir(parents=True, exist_ok=True)`：确保 data/logs/screenshots 目录存在
- 整个程序生命周期内只创建一次 `Settings` 对象

**对比另一种单例写法**（模块级变量）：
```python
# Python 模块只会加载一次，所以模块级变量天然就是单例
settings = Setting()
# 使用：from core.config import settings
```
区别：模块级写法更简洁，但无法在创建前做初始化逻辑（如创建目录）。

## 异常体系-core/exceptions.py

## 日志系统-core/logger.py

## 数据结构定义-core/schemas.py
**核心数据结构**：
- action指令
- 感知结构
- 任务/步骤

- 统一的数据契约
    - 配合pydantic自动校验，非法数据在构造时就会报错，不至于运行时出类型错误
    - is_terminal() 方法：`done`和`error`是终态动作，状态机收到后停止循环

## 数据库持久化-core/storage.py

## 冒烟测试
**做什么：**验证核心层所有组件能正常工作
在webagent/core和webagent中增加__init__.py
在main.py中增加自测命令
测试：
```
python -m webagent self-check
```

### 浏览器执行层executor
playwright，通过代码操作浏览器
- 打开/关闭浏览器
- 导航到指定网址
- 点击界面元素
- 输入文本
- 获取截图
两种启动模式
- launch
- cdp：先启动浏览器并添加参数，--remote-debugging-port=9222
```
playwright install chromium
```
chromium是浏览器内核

## 浏览器管理——executor/browser.py
- 浏览器的启动、接管
- 窗口控制
- 资源释放

```
访问器：是指带有健康检查的页面访问，如果页面在agent的行为过程中被用户关闭或者其他意外关闭，需要重开一个界面
@property：装饰器的用法，简化方法调用，不需要添加()
__exit__：用于with BrowserManager() as browser的时候可以自动关闭，并释放资源
```

## 动作执行引擎——executor/actions.py
双兜底，支持10种动作类型
- DOM选择器优先：精准，快速，可靠
- 视觉坐标兜底：iframe嵌套和动态渲染等可能导致DOM失效。截图坐标pyautogui点击等动作
- 重试机制：每个动作都可以配置重试次数，应对偶发的元素未加载问题

    "click",
    "input",
    "goto",
    "press_key",
    "wait",
    "scroll",
    "back",
    "refresh",
    "done",
    "error"

```python
handler={
            "click":self._click,
            "input":self._input,
            "goto":self._goto,
            "press_key":self._press_key,
            "wait":self._wait,
            "scroll":self._scroll,
            "back":self._back,
            "refresh":self._refresh,
            "done":lambda _a:True,
            "error":lambda _a:True,
        }.get(action.action_type)

```
handler用字典映射，接受一个函数对象，否则得写一堆if-else
```python
# ❌ 不用字典映射：10种动作=10个分支，每加一种动作都要回来改主入口
if action.action_type == "click":
    self._click(action)
elif action.action_type == "input":
    self._input(action)
elif action.action_type == "goto":
    self._goto(action)
elif action.action_type == "press_key":
    self._press_key(action)
elif action.action_type == "wait":
    self._wait(action)
elif action.action_type == "scroll":
    self._scroll(action)
elif action.action_type == "back":
    self._back(action)
elif action.action_type == "refresh":
    self._refresh(action)
elif action.action_type in ("done", "error"):
    pass  # 终态动作，无需实际操作
else:
    raise ActionExecutionError(f"未知action_type: {action.action_type}")

# ✅ 字典映射后：查表取函数 → 判空兜底 → 统一调用
# self._click 不加括号：存的是函数对象本身；lambda _a: True 给终态动作占位
if handler is None:
    raise ActionExecutionError(f"未知action_type: {action.action_type}")
handler(action)
```

为什么refresh和back传入参数，a前面加一个下划线？因为python的命名规则，这个参数我收到了，但是实际不使用；为什么不直接舍弃？因为需要保持与Handler字典映射的接口一致，避免调用时报错。
```python
        def _refresh(self,_a:Action)->None: # 忽略a参数。只是放在这，实际不使用
            self.page.reload(wait_until="domcontentloaded",timeout=30_000)

        def _back(self,_a:Action)->None: # 忽略a参数。只是放在这，实际不使用
            self.page.go_back(wait_until="domcontentloaded",timeout=30_000)
```

# 感知层
- **双模态感知**

## 页面感知采集器——perception/collector.py
    - DOM摘要
    - 截图
    - 双模态：多模态大模型同时看到文字和画面，更为精准决策
在浏览器中注入JS，采集可交互的元素，同时截图，打包为pagesnapshot：页面url/title/文本摘要/视口内可交互元素的列表（selector和bbox），截图（base64编码的png图片）
设计要点：
    - 1. 稳定selector，优先使用元素自带的标签 id/data-*/aria-label，css=[data-wa-id="<index>"]保证通过注入可以100%命中
    - 2. 面积>0，未被遮挡的元素，getClientReacts()非空
    - 3. 数量上限：超出agent_dom_max_elements时按排序（视口内，面积，dom顺序）后截断，token不要过长

## 方法、类方法、静态方法

三者的核心区别只有一个：**Python 调用时自动传入的第一个参数是什么**

```python
class Demo:
    version = "1.0"              # 类属性（所有实例共享）
    def __init__(self):
        self.name = "实例属性"     # 实例属性（每个实例各一份）

    def instance_method(self):        # 实例方法：自动传 self（实例本身）
        return self.name              # 实例属性、类属性都能访问

    @classmethod
    def class_method(cls):            # 类方法：自动传 cls（类本身）
        return cls.version            # 只能访问类属性，拿不到 self.name

    @staticmethod
    def static_method():              # 静态方法：什么都不自动传
        return "我就是个普通函数"       # self 和 cls 都没有

d = Demo()
d.instance_method()    # self 自动 = d
Demo.class_method()    # cls 自动 = Demo（用 d.class_method() 调也行，cls 还是 Demo）
Demo.static_method()   # 和调用普通函数一模一样 
```

### 对比表

| 类型 | 装饰器 | 自动传入 | 能访问 | 典型用途 |
| ---- | ---- | ---- | ---- | ---- |
| 实例方法 | 无 | self（实例） | 实例属性 + 类属性 | 绝大多数业务逻辑 |
| 类方法 | @classmethod | cls（类本身） | 只有类属性 | 替代构造函数（工厂方法） |
| 静态方法 | @staticmethod | **什么都不传** | 都不访问 | 不碰状态的纯计算工具函数 |

### 项目里的真实例子

**实例方法**：executor/actions.py 的 `execute(self, action)`
必须拿到 `self.page` 才能操作浏览器、`self.cfg` 读重试配置——依赖实例状态，所以是实例方法。

**静态方法**：perception/collector.py 的 `_text_context`（截断文本摘要用）
只做纯字符串计算，不读任何 self 属性。加 `@staticmethod` 是一种**自文档**：一眼告诉读代码的人"这个函数不碰实例状态，随便测"。

### 静态方法存在的意义

这个函数逻辑上属于这个类（是感知层的工具），但不依赖任何实例/类状态——放进类里比丢在模块顶层更内聚。也可以理解为：**静态方法 = 被类收编的普通函数**。

### 类方法最经典的用法：替代构造函数（工厂方法）

```python
class Action(BaseModel):
    @classmethod
    def from_json(cls, raw: str) -> "Action":
        # 用 cls 而不是写死 Action，子类调用时返回的就是子类实例
        return cls.model_validate_json(raw)

# Action.from_json('{"action_type":"click",...}')  -> Action
# SafeAction.from_json(...)                        -> SafeAction（子类不丢类型）
```

### 两个容易混的点

- `self`/`cls` 不是关键字，只是约定俗成的参数名——和 `_a` 一样，**名字没有魔力，位置才重要**（第一个参数位）
- 通过实例调用静态方法时，self 不会被传进去：`self._text_context(text)` 只传了 `text`。所以 handler 字典里 `self._click` 会自动绑定 self，而静态方法不会——区别就在这

### 决策层agent

#### 工作流程
- 1. ai收到任务描述
- 2. ai看到dom和截图
- 3. ai思考并且输入一个json
- 4. 执行层拿到json，执行对应的操作

#### System Prompt (不要重复内容，不能有歧义，简洁整齐合理)
- 设定角色：你是一个一流的web agent
- 说明输入： 你会看到任务/历史/页面元素/截图
- 规定输出：你必须输出json，格式如下...
- 给示例：示例1：... 示例2：...
- 定规则：任务完成输出done，错误输出error

#### 状态机
前置知识：langchain/langgraph，开发agent常用第三方库。
agent=LLM+工具调用+上下文管理
状态机类似于一个红绿灯：一个写死的轮换流程，没有条件选择和回退机制
```
红灯->绿灯->黄灯->红灯... 
```
agent的状态机
```
空闲->感知页面->思考->执行动作->检查结果->继续感知or完成
```

#### agent的多模态大模型：
url+api-key+模型名称
temperature=0.2，重试次数
最大步数：防止无限循环
ai输出格式不对的话需要有相应的容错机制

**状态机才是真正负责流程控制，确保按照 感知->推理->执行 的循环顺序再走**
LLM负责业务决策，看来截图，决定下一步action

Langgraph和状态机transitions库
| 对比维度 | 本项目 (`transitions` 库) | LangGraph |
| --- | --- | --- |
| **复杂度** | 轻量级有限状态机（FSM） | 复杂的图结构状态管理 |
| **状态定义** | 硬编码 7 个固定状态 | 动态图节点，可任意扩展 |
| **控制方式** | 代码显式调用 `self.start()` → `self.perceived()` 触发状态切换 | LLM 返回结果自动决定下一个节点 |
| **状态内容** | 只存"现在处于哪个阶段" | 存完整上下文（对话历史、工具结果、页面信息等） |
| **LLM 是否看状态** | **不看**，LLM 只看页面快照和历史 Action | **看**，LLM 根据 State 内容决定路由 |
| **适合场景** | 线性循环（感知→推理→执行→循环） | 多分支、条件路由、并行执行、动态决策 |
| **灵活性** | 低（改流程要改代码） | 高（LLM 可以在节点间自由跳转） |

**本项目状态机**：
```python
# 状态是固定的 7 个，写死在代码里
_STATES = ["idle", "perceiving", "reasoning", "executing", "checking", "done", "failed"]

# 转换规则是硬编码的
transitions=[
    {"trigger": "start", "source": "idle", "dest": "perceiving"},
    {"trigger": "perceived", "source": "perceiving", "dest": "reasoning"},
    # ... 每个转换都写死
]
```

**线性循环**：

```
idle → perceiving → reasoning → executing → checking → perceiving → ...
                                                        ↓
                                                     done/failed
```

**代码示例**：

```python
from langgraph.graph import StateGraph

# 状态是一个对象，包含所有上下文信息
class AgentState(TypedDict):
    messages: list              # 对话历史
    current_page: str           # 当前页面信息
    actions_taken: list         # 已执行的动作
    task_progress: str          # 任务进度

# 节点可以自由组合
workflow.add_node("analyze", analyze_node)
workflow.add_node("act", act_node)
workflow.add_node("reflect", reflect_node)

# LLM 通过条件边决定下一步去哪
workflow.add_conditional_edges("analyze", router_function, {
    "need_action": "act",
    "need_more_info": "reflect",
    "done": END
})
```
```
       analyze
      /    |    \
   act  reflect  END
    |      |
    └──────┘
    (可以跳回任意节点)
```

## 多模态大模型客户端——agent/llm_client.py
封装openai兼容的多模态大模型，输入任务+感知层的dom或截图，输出标准Action JSON

1. tenacity重试：指数退避（1s->2s->4s->8s...），最多重试3次
2. **Reflexion 机制**（反思/进度提示）：
    - 上一步失败 → 告诉模型失败原因，提示换思路
    - 上一步成功 → 提示检查任务是否已完成
    - 页面 URL/标题变化 → 帮助模型判断动作是否生效
    - 完成守则 → 防止模型在任务已完成时继续多余操作
3.System Prompt 是核心：
    - 明确角色定义（"世界一流的 Web Agent"）
    - 列出所有可用动作和参数格式
    - 给出输入输出示例（few-shot learning）
    - 强制 JSON 输出，禁止自由文本

## Agent自主循环状态机——agent/loop.py

1. transitions库：定义装填和状态跳转的规则
2. 三重防御：
    - max_steps：硬上限，（默认设置25，本项目设置为15）
    - 死循环测试：同一动作+执行成功+页面无变化->连续3次终止
    - 取消支持：threading.Event()跨线程通信实现
3. **Reflexlexion 信息传递**：
    - last_error：帮助大模型转换思路
    - prev_url/prev_title:帮助大模型判断页面是否发生变化
4. **每步截图**：执行之后截图，帮助事后回溯

